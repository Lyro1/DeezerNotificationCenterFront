import { NotificationTypeToDisplayPipe } from './notification-type-to-display.pipe';

describe('NotificationTypeToDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new NotificationTypeToDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
